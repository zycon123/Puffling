# beta.104 completion criteria

beta.104 is complete when acquisition source uniqueness, reward allowlisting, replay safety, and atomic settlement are implemented; regression/static tests pass; the full build and Race Server workflows are green; migration safety is reviewed; and the PR is explicitly approved for merge.