# beta.104 next commit contents

The next commit group should activate the atomic service in production code and update validation accordingly. After that, open a draft PR against `main` and inspect CI. Any failure should be fixed on beta.104 without touching the green beta.103 main branch.
