# beta.104 release gate

A beta.104 pull request may be marked ready only after the final branch head passes both GitHub Actions validation workflows and review confirms the source-level uniqueness migration is non-destructive for existing data.

Merge into `main` requires explicit approval after those checks. Until then beta.103 remains the release branch state.