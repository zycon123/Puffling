# beta.104 internal release note draft

Boss rewards are being hardened so the server owns the complete settlement boundary. Reward IDs are allowlisted, conflicting replay attempts are rejected, and the new settlement path is designed to update inventory, reward receipt and Boss Session consumption together. No new paid infrastructure is introduced.
