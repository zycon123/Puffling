# beta.104 stop point

Safe checkpoint reached before production routing changes. The next continuation can begin directly with bootstrap and acquisition HTTP wiring. No beta.104 code is active on `main` yet.
