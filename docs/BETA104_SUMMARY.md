# beta.104 summary

beta.104 hardens the server-authoritative boss reward path introduced in beta.103. The main additions are canonical reward allowlisting, source-level receipt uniqueness, stronger replay/concurrency resolution, and an exact proof-consumption check inside the same transaction as inventory and receipt writes.

The phase remains zero-cost and deliberately does not claim fully server-simulated boss combat.