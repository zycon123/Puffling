import fs from 'node:fs';import vm from 'node:vm';
const fail=m=>{throw new Error(m)};let active='volt',usable={ember:true,volt:true},seen=[],setCalls=[],energyLosses=[],results=[];
const state={owned:{ember:1,volt:1}},F={load:()=>({owned:{...state.owned}})},G={active:()=>active,setActive:id=>{setCalls.push(id);active=id;return true}};
const status={textContent:''},ctx={window:null,console,setTimeout:fn=>{fn();return 0},showToast:()=>{},document:{getElementById:id=>id==='multiplayerStatus'?status:null},SkyPuffFusion:F,SkyPuffPufflingGameplay:G,OrbuffEnergy:{canUse:id=>!!usable[id],consumeLoss:id=>{energyLosses.push(id);return {ok:true,energy:4,max:5}}},SkyPuffRaceUI:{showResult:s=>results.push(s)},startMultiplayerRace:async type=>{seen.push({type,active:F.load().active});return true}};ctx.window=ctx;vm.createContext(ctx);vm.runInContext(fs.readFileSync('js/race_active_orbuff_guard.js','utf8'),ctx,{filename:'js/race_active_orbuff_guard.js'});
if(!ctx.OrbuffRaceActiveGuard)fail('Race active Orbuff guard API missing');
await ctx.startMultiplayerRace('random');if(seen.at(-1)?.active!=='volt')fail(`Race ignored selected active Orbuff: ${JSON.stringify(seen.at(-1))}`);ctx.SkyPuffRaceUI.showResult({winner:'rival'});ctx.SkyPuffRaceUI.showResult({winner:'rival'});if(energyLosses.length!==1||energyLosses[0]!=='volt')fail('Race loss did not consume exactly one energy from the participating Orbuff');
usable.volt=false;await ctx.startMultiplayerRace('friend');if(active!=='ember'||seen.at(-1)?.active!=='ember'||setCalls.at(-1)!=='ember')fail('Race did not switch away from an unavailable active Orbuff');ctx.SkyPuffRaceUI.showResult({winner:'you'});if(energyLosses.length!==1)fail('Race win incorrectly consumed energy');ctx.SkyPuffRaceUI.showResult({winner:'rival'});if(energyLosses.length!==2||energyLosses[1]!=='ember')fail('Fallback Orbuff Race loss did not consume its energy');
usable.ember=false;const before=seen.length,result=await ctx.startMultiplayerRace('random');if(result!==false||seen.length!==before||!status.textContent.includes('Ingen Orbuff'))fail('Race was not blocked when every owned Orbuff was exhausted/vaulted');
console.log('✅ Race uses selected usable Orbuff, falls back safely, blocks unavailable rosters and charges one energy per loss');