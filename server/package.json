{
  "name": "puffling-race-server",
  "version": "0.4.0",
  "private": true,
  "description": "Authoritative WebSocket server for Puffling Race, trades and Diamond wallet",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check trade.js && node --check iap_store.js && node --check integration-check.js && node integration-check.js && node iap-check.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "ws": "^8.18.0"
  }
}
